<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MyCustomTabLayout" parent="Widget.Design.TabLayout">
        <item name="tabTextAppearance">@style/MyCustomTabText</item>
        <item name="tabIndicatorColor">@color/cyanBlue</item>
        <item name="tabSelectedTextColor">@color/cyanBlue</item>
        <item name="tabTextColor">@color/lightGreyBold</item>
    </style>

    <style name="MyCustomTabText" parent="TextAppearance.Design.Tab">
        <!--<item name="android:textSize">12sp</item>-->
        <item name="android:textStyle">bold</item>
        <item name="textAllCaps">false</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textSize">15sp</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="SearchTheme" parent="Widget.AppCompat.Light.SearchView">
        <item name="searchIcon">@drawable/ic_search_black_24dp</item>
        <item name="searchHintIcon">@drawable/ic_search_black_24dp</item>
        <item name="closeIcon">@drawable/ic_exit_control_voice_black</item>
        <item name="queryBackground">@drawable/solid</item>
    </style>
</resources>
